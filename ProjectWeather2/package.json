{
  "name": "weather",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "jest --silent",
    "prepare": "husky install"
  },
  "dependencies": {
    "jquery": "^3.3.1",
    "serve": "^14.1.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0",
    "jest": "^29.0.0",
    "jsdom": "^22.0.0"
  },
  "lint-staged": {
    "*.{js,json,css,html}": ["prettier --write", "eslint --fix"]
  }
}
